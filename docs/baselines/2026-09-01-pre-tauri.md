# Baseline antes da migração para Tauri

Registrado em 2026-09-01 no repositório `/home/david/mega-brain`, antes de qualquer extração do backend ou adição do shell desktop.

## Comandos executados

### `npm test`

Resultado: **falhou** (código de saída 1) sem coletar testes.

O Vitest 2.1.9 tentou criar seus diretórios temporários em
`/mnt/c/Users/david/AppData/Local/Temp/2bknR1vo_KrkKoakkDbad`, que não existe neste ambiente WSL. Foram reportados 20 erros `ENOENT` idênticos, com o resumo:

```text
Test Files  no tests
     Tests  no tests
    Errors  20 errors
```

Este é o estado conhecido do baseline; a causa deve ser tratada separadamente para que a suíte consiga executar no ambiente atual.

### `npm run build`

Resultado: **passou** (código de saída 0).

O comando executou `tsc -b && vite build` com Vite 6.4.3 e concluiu em 9,28 s. O maior bundle JavaScript produzido foi `dist/assets/index-D4o5GPNH.js` (2.092,77 kB; 634,92 kB gzip).

O Vite emitiu o aviso esperado de chunk acima de 500 kB após minificação; isso não impediu o build.
